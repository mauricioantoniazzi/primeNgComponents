import { TreeComponent } from './tree.component';
import { RouterModule, Routes } from '@angular/router';
import { NgModule } from '@angular/core';

export const TreeRoutingModule: Routes = [
  { path: '', component: TreeComponent }
];
